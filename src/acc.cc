#include <iostream>
#include "acc.h"
#include "pid.h"

/* ACC(speed, set_speed, acceleration, gas, brake, lead_distance, set_distance)
 *
 *
 *
 *
 */

ACC::ACC(double speed, double set_speed, double acceleration, double gas, double brake, double lead_distance, double set_distance, bool mode) { }



void GetDistance(
